
import { restaurantPopularity } from 'crowd-sourced-review-site/helpers/restaurant-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | restaurant popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = restaurantPopularity([42]);
  assert.ok(result);
});

